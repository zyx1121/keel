image: ghcr.io/owner/app
service: app            # systemd unit name OR docker compose service name
health_url: http://localhost:8080/health
health_timeout: 30      # seconds to wait for health_url to return 200
env_file: /etc/keel/app.env   # host-side env file; never in the repo
runtime: compose        # compose | systemd-container
